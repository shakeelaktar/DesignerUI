# Point the Designer UI to your ApiGateway base URL (optional). Must include scheme and host.
# When unset, the UI will call relative paths (same origin) like /devportal/audit/...
# Example values:
# VITE_GATEWAY_BASE=http://localhost:8080
# VITE_GATEWAY_BASE=http://host.docker.internal:8080
VITE_GATEWAY_BASE=

# Optional: Point sign-in calls to your Identity API base URL.
# When unset, dev server proxy will use http://localhost:5000
# Example values:
# VITE_IDP_BASE=http://localhost:5000
# VITE_IDP_BASE=http://host.docker.internal:5000
VITE_IDP_BASE=
